[project]
name = "rule-scout"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "httpx>=0.27.2",
    "httpx-retries>=0.5.0",
    "lxml>=6.1.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.11",
]
